<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.code.sys.mapper.CodeMapper">

    <select id="getByCode" resultType="com.code.sys.entity.Code">
        SELECT * FROM sys_code
        WHERE create_time >= DATE_SUB(NOW(), INTERVAL 5 MINUTE)
        and email = #{email} and code = #{code} limit 1
    </select>
</mapper>
